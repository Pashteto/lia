# Design

Design explorations and mockups for the Lia frontend.

- `stitch_/` — Stitch export with frontend design variants (5 desktop directions, mobile, and a curatorial-minimalist direction). Each variant includes a screenshot (`screen.png`) and generated HTML (`code.html`).

These are reference material for implementing the [`../frontend/`](../frontend/) — the source design brief is [`../docs/design_agent_prompt.md`](../docs/design_agent_prompt.md).
