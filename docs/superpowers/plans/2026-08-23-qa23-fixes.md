# QA-23-aug Fixes Implementation Plan

> **STATUS: EXECUTED, DEPLOYED LIVE, RE-TESTED — 2026-08-23.** Все 18 задач
> выполнены (инлайн, worktree `qa23-fixes`, 17 коммитов → `main` `a92e651`,
> pushed). Прод: `lia-backend:qa23-r1` + `lia-frontend:qa23-r2`, rollback
> `*:rollback-qa23-20260823`. Итоги и отклонения — в раннбуке
> `docs/superpowers/runbooks/2026-08-23-qa23-fixes-deploy.md`.
>
> **Отклонения от плана:**
> - Task 2: у swagger-опа SignUp нет 403-респондера — owner-guard отдаёт **409**
>   с русским сообщением (регенерить спеку ради кода не стали).
> - Task 3: параметр `from` у `/events/nearby` не добавлялся (тоже требовал
>   регенерации спеки) — фильтр «не раньше московской полуночи» зашит в SQL
>   безусловно; фронтовый параметр не понадобился.
> - Task 4: payload-поля добавлены ПРАВИЛЬНЫМ путём — через `api/swagger.yaml`
>   + `make generate-api` (генерённый код gitignored, это штатный workflow).
> - Task 13/15: найден и поправлен ТРЕТИЙ статичный копирайт (десктопный рейл
>   мастера «После отправки») — в плане его не было.
> - Пост-план hotfix (frontend r2): `failMe` из Task 9 сносил сессию по 401 от
>   `/auth/me`, а бэкенд отвечает 401 сразу после регистрации → откатили
>   clearSession, сохранив баннер-фикс.
> - Task 18 вскрыл корень бага №2/№3: глобальный `organizers.auto_verify_all`
>   был включён с 26.07 — **выключен через /admin/settings** (продуктовое
>   изменение конфигурации, без него гейт Task 1 ни на ком не срабатывал).

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix QA-run findings №3–12 + organizer/viewer UX features, redeploy to prod, re-test.

**Architecture:** Go modular monolith (`backend/`, go-swagger HTTP layer + hand-rolled mux modules) + Next.js App Router frontend (`frontend/`). Backend gets a pre-moderation gate for unverified organizers, an owner-RSVP guard, a date filter on `/events/nearby`, and a `pending_applications_count` enrichment. Frontend gets `is_owner`-driven owner panel + CTA suppression, form-error visibility, verify-funnel deep-link, `/map` height fix, mobile header/nav cleanup, «Я»-hub organizer section, human status labels, follower-count refresh, and URL redirects.

**Tech Stack:** Go 1.22 (std `testing`), Next.js 15 / React, Tailwind (Swiss-Grid tokens: `border-ink`, `bg-paper`, `text-signal`), vitest **without jsdom** (tests render via `renderToStaticMarkup` or test pure lib functions), react-query.

**Spec:** `docs/superpowers/runbooks/2026-08-23-full-role-qa-run.md` (bugs №3–12 + «фичи» section; user message 2026-08-23 pins the exact scope).

## Global Constraints

- All user-facing copy in Russian; match existing tone (short, no exclamation).
- No new DB migrations (every backend change works on prod schema 26).
- Do not edit go-swagger generated files (`internal/http/server/`, `embedded_spec.go`, `operations/`); change only handlers/formatter/domain packages.
- Frontend tests: vitest has **no jsdom** — test pure functions in `lib/`, or components via `renderToStaticMarkup` (see `components/__tests__/owner-event-actions.test.tsx` for the pattern).
- Backend tests: table-driven std `testing`, colocated `*_test.go`; DB-backed tests only in `*_integration_test.go` (skip if no DB).
- Commit after every task (conventional commits: `fix:` / `feat:` / `test:`).
- Verify commands: backend `cd backend && go build ./... && go test ./...`; frontend `cd frontend && npx vitest run && npx next build` (build only in Task 16).
- Prod deploy: build linux/amd64 on Mac → `docker save | ssh vdska2 docker load`; **tag `:latest` before recreate** (compose pulls `:latest`); rollback tags from the RUNNING container id; frontend needs BOTH build args `NEXT_PUBLIC_API_URL=https://api.presence.tarski.ru` and `NEXT_PUBLIC_YANDEX_MAPS_KEY`.

---

### Task 1: Backend — pre-moderation gate for unverified organizers (bug №3)

**Files:**
- Modify: `backend/internal/events/service.go` (constructor + `Create` ~L281, `Update` status block L421–434)
- Modify: `backend/internal/http/module.go` (wiring, ~L296–330)
- Test: `backend/internal/events/service_test.go`

**Interfaces:**
- Consumes: `organizers` service — find its existing method to fetch an organizer by owner user id (repository has `GetByOwner`-style lookup, verification transitions at `internal/organizers/repository.go` L95–170). If the service lacks a public method, add `func (s *Service) IsVerifiedOwner(ctx context.Context, ownerUserID uuid.UUID) (bool, error)` returning `verification_status == "verified"` (missing organizer row → `false, nil`).
- Produces: `events.OrganizerVerifier` interface consumed by the events service; behavior later relied on by Tasks 13 (copy) and 17 (retest): unverified organizer publishing → event lands in `pending_review`, `moderation_required=true` in the API payload (formatter already does this at `internal/http/formatter/event.go` L126).

- [ ] **Step 1: Write the failing tests** in `backend/internal/events/service_test.go`. Follow the file's existing fake/stub pattern for dependencies; add a stub verifier:

```go
type stubVerifier struct{ verified bool; err error }

func (s stubVerifier) IsVerifiedOwner(ctx context.Context, owner uuid.UUID) (bool, error) {
	return s.verified, s.err
}

func TestPublishByUnverifiedOrganizerGoesToPendingReview(t *testing.T) {
	// arrange service with stubVerifier{verified: false}
	// act: Create with Status "published" (or Update draft→published)
	// assert: stored event.Status == models.EventPendingReview
}

func TestPublishByVerifiedOrganizerStaysPublished(t *testing.T) {
	// stubVerifier{verified: true} → event.Status == models.EventPublished, PublishedAt set
}

func TestEditOfAlreadyPublishedEventNotDemoted(t *testing.T) {
	// event already published, unverified organizer patches title only (p.Status == nil)
	// → status stays EventPublished
}

func TestWithdrawToCancelledSkipsGate(t *testing.T) {
	// unverified organizer, p.Status = "cancelled" → EventCancelled (no pending_review)
}
```

- [ ] **Step 2: Run** `cd backend && go test ./internal/events/ -run 'PendingReview|StaysPublished|NotDemoted|SkipsGate' -v` — expect FAIL (interface/method missing).

- [ ] **Step 3: Implement.** In `service.go`:

```go
// OrganizerVerifier reports whether the event owner passed admin verification.
type OrganizerVerifier interface {
	IsVerifiedOwner(ctx context.Context, ownerUserID uuid.UUID) (bool, error)
}
```

Add `orgVerifier OrganizerVerifier` to the service struct + constructor. Add:

```go
// applyModerationPolicy demotes a to-be-published event of an unverified
// organizer to pending_review. Call ONLY on a draft/pending→published
// transition, never on edits of an already-published event.
func (s *service) applyModerationPolicy(ctx context.Context, event *models.Event) error {
	if event.Status != models.EventPublished {
		return nil
	}
	verified, err := s.orgVerifier.IsVerifiedOwner(ctx, event.OrganizerID)
	if err != nil {
		return err
	}
	if !verified {
		event.Status = models.EventPendingReview
		event.PublishedAt = nil
	}
	return nil
}
```

Call it: in `Create` right after `applyExternalURLPolicy` (~L281); in `Update` inside the `if p.Status != nil` block (L421–434) **only when the prior status was not published** (capture `prev := event.Status` before overwriting). Order: external-URL policy first, then moderation policy (both can independently demote to `pending_review`; that's fine).

- [ ] **Step 4: Wire it** in `internal/http/module.go`: pass the organizers service (already constructed there for the organizers mux handler) into the events service constructor. If organizers service init happens after events, reorder.

- [ ] **Step 5: Run** the same tests — expect PASS. Then full `go build ./... && go test ./...`.

- [ ] **Step 6: Check the admin queue picks these up.** `internal/moderation` lists `pending_review` events; admin UI tab «ССЫЛКИ · 0» on `/admin/moderation/events` filters them (frontend). In `frontend/` grep for `ССЫЛКИ` / the pending filter; rename the tab label to `НА ПРОВЕРКЕ` (it now holds all pre-moderation, not just external links). Approve endpoint `POST /api/v1/admin/moderation/events/{id}/approve` needs no change (repo already flips `pending_review→published`).

- [ ] **Step 7: Commit** `feat(moderation): events from unverified organizers go to pending_review before publish`

---

### Task 2: Backend — owner cannot RSVP/apply to own event (bug №5)

**Files:**
- Modify: `backend/internal/rsvp/service.go` (`SignUp` L111–135, `ErrForbidden` already declared L21)
- Modify: `backend/internal/http/handlers/rsvp.go` (`SignUp.Handle` error mapping L62–75)
- Test: `backend/internal/rsvp/service_test.go`

**Interfaces:**
- Consumes: `event.OrganizerID` from the already-loaded event inside `service.SignUp`.
- Produces: `ErrForbidden` on owner self-signup → HTTP 403 with body `{"code":"owner_rsvp_forbidden","message":"Нельзя записаться на собственное событие"}`. Task 6 (frontend) hides the CTA; this is the defense-in-depth layer.

- [ ] **Step 1: Failing test** in `service_test.go` (reuse the file's fixture style):

```go
func TestSignUpOwnEventForbidden(t *testing.T) {
	// event.OrganizerID == userID
	_, err := svc.SignUp(ctx, eventID, ownerID, "")
	if !errors.Is(err, ErrForbidden) {
		t.Fatalf("want ErrForbidden, got %v", err)
	}
}
```

- [ ] **Step 2: Run** `go test ./internal/rsvp/ -run OwnEventForbidden -v` — FAIL.

- [ ] **Step 3: Implement** in `service.SignUp` right after the event is loaded, before the `SignupMode` switch:

```go
if event.OrganizerID == userID {
	return nil, ErrForbidden
}
```

- [ ] **Step 4: Map in handler** `SignUp.Handle` error switch (alongside `ErrNotFound→404`): `ErrForbidden` → 403, message «Нельзя записаться на собственное событие». Mirror whatever responder-construction pattern the surrounding cases use.

- [ ] **Step 5: Run** rsvp package tests + `go test ./...` — PASS.

- [ ] **Step 6: Commit** `fix(rsvp): forbid organizer self-signup on own event`

---

### Task 3: Backend — `/events/nearby` excludes past events (bug №12, map «Всего 7» vs лента 5)

**Files:**
- Modify: `backend/internal/events/repository.go` (`Nearby` L320–363, SQL status predicate at L330)
- Modify: `backend/internal/events/service.go` (`Nearby` L552–564), `backend/internal/http/handlers/events_nearby.go` (L25+)
- Modify: `frontend/lib/api.ts` (`fetchNearbyEvents` L245–259), `frontend/components/MapBrowse.tsx` (`load()` L79–99)
- Test: `backend/internal/events/repository_test.go` (only if it has non-integration SQL assertions; otherwise handler test), `frontend/lib/__tests__/feed-window.test.ts` pattern for the client param

**Interfaces:**
- Consumes: `feedWindowStart()` from `frontend/lib/api.ts` L175–179 (local midnight).
- Produces: `GET /api/v1/events/nearby?lat&lon&limit&from=<RFC3339>` — optional `from`; when present, SQL adds `AND e.starts_at >= $from`.

- [ ] **Step 1: Backend failing test.** If `repository_test.go` is integration-only, test the handler param parsing instead in `internal/http/handlers/` tests: `from` query propagates to service call; bad `from` → 400. Follow `events_test.go` fixtures.

- [ ] **Step 2: Run it** — FAIL.

- [ ] **Step 3: Implement backend.** Thread `from *time.Time` through handler → `service.Nearby` → `repository.Nearby`; in the SQL builder add:

```go
if from != nil {
	sb.WriteString(" AND e.starts_at >= $N")
	args = append(args, *from)
}
```

(match the query-building idiom already used in `List` L237.)

- [ ] **Step 4: Frontend.** `fetchNearbyEvents(lat, lon, limit)` → add `params.set("from", feedWindowStart().toISOString())` (same helper the feed uses at L193–209). No `MapBrowse` change needed beyond the call already going through this function.

- [ ] **Step 5: Run** `go test ./...` and `npx vitest run` — PASS.

- [ ] **Step 6: Commit** `fix(map): nearby events exclude past — map and feed now agree`

---

### Task 4: Backend — `is_owner` on event payload + `pending_applications_count` on /events/mine

**Files:**
- Modify: `backend/internal/http/formatter/event.go` (add fields), `backend/internal/http/models/event.go` (payload struct)
- Modify: `backend/internal/http/handlers/events.go` (detail handler — `isOwner` already computed at L172, pass it into the formatter)
- Modify: `backend/internal/rsvp/repository.go` (new count query), `backend/internal/events/…` mine-list handler (find the handler serving `GET /events/mine`; it lives beside `ListEvents` — grep `mine`)
- Test: `backend/internal/rsvp/repository_test.go` or service test with fake repo; formatter test if one exists

**Interfaces:**
- Produces (JSON): event detail gains `"is_owner": bool` (true only for the authed owner; omit/false otherwise). `/events/mine` rows gain `"pending_applications_count": int` (count of rsvps with `status='applied'`).
- Consumed by: Task 6 (owner panel/CTA), Task 12 (заявки counter chip).

- [ ] **Step 1: Failing test** for the count query (fake-repo service test or integration guard):

```go
func TestCountPendingApplicationsByEvent(t *testing.T) {
	// two events; event A has 2 applied + 1 going; event B has 0 applied
	// want map[A]=2, B absent or 0
}
```

- [ ] **Step 2: Run** — FAIL.

- [ ] **Step 3: Implement** in `rsvp/repository.go`:

```go
// CountPendingApplications returns applied-rsvp counts per event.
func (r *repository) CountPendingApplications(ctx context.Context, eventIDs []uuid.UUID) (map[uuid.UUID]int, error) {
	rows, err := r.db.QueryContext(ctx,
		`SELECT event_id, COUNT(*) FROM event_rsvps
		  WHERE event_id = ANY($1) AND status = 'applied'
		  GROUP BY event_id`, pq.Array(eventIDs))
	// scan into map, follow the package's existing pq/pgx idiom
}
```

Expose through the rsvp service; call it from the mine-list handler enrichment (same place seats are attached) and set the new payload field. In the detail handler pass `isOwner` (L172) into the formatter; formatter sets `out.IsOwner = isOwner` (add param or setter — keep generated models untouched, extend only the hand-written payload struct in `internal/http/models/event.go` L324 area).

- [ ] **Step 4: Run** package tests + `go test ./...` — PASS.

- [ ] **Step 5: Commit** `feat(api): is_owner on event detail, pending_applications_count on /events/mine`

---

### Task 5: Frontend — map `is_owner`/counts into the Lia event model

**Files:**
- Modify: `frontend/lib/api.ts` (`apiEventToLia` L23–88; `fetchMyEvents` L226 row mapping)
- Modify: the Lia event type (where `myRsvpStatus` is declared — `frontend/lib/types.ts` or wherever `apiEventToLia` returns; grep `myRsvpStatus`)
- Test: `frontend/lib/__tests__/` new `api-event-owner.test.ts`

**Interfaces:**
- Produces: `event.isOwner?: boolean`, `event.pendingApplicationsCount?: number` on the frontend event objects. Consumed by Tasks 6 and 12.

- [ ] **Step 1: Failing test:**

```ts
import { describe, expect, it } from "vitest";
import { apiEventToLia } from "@/lib/api";

it("maps is_owner and pending_applications_count", () => {
  const ev = apiEventToLia({ id: "x", title: "t", starts_at: "2026-09-30T19:00:00Z",
    is_owner: true, pending_applications_count: 3 } as never);
  expect(ev.isOwner).toBe(true);
  expect(ev.pendingApplicationsCount).toBe(3);
});
```

(If `apiEventToLia` isn't exported, export it — the file already exports mapping helpers for tests like `feed-window`.)

- [ ] **Step 2: Run** `npx vitest run lib/__tests__/api-event-owner.test.ts` — FAIL.

- [ ] **Step 3: Implement** the two fields in `apiEventToLia` + type. Default `false`/`0` when absent.

- [ ] **Step 4: Run** — PASS. **Step 5: Commit** `feat(front): event model carries isOwner and pendingApplicationsCount`

---

### Task 6: Frontend — owner panel on published events + hide RSVP CTA from owner (bugs №5, №6)

**Files:**
- Modify: `frontend/lib/owner-actions.ts` (`ownerPanelMode`)
- Modify: `frontend/components/OwnerEventActions.tsx` (L15–38)
- Modify: `frontend/components/SignupCTA.tsx` (top of render, ~L96–110)
- Test: `frontend/components/__tests__/owner-event-actions.test.tsx` (extend), new `frontend/lib/__tests__/owner-actions.test.ts`

**Interfaces:**
- Consumes: `event.isOwner` from Task 5; `event.status`.
- Produces: `ownerPanelMode(status, isOwner)` → `"draft" | "pending" | "published" | null`. SignupCTA renders an owner note instead of signup controls when `event.isOwner`.

- [ ] **Step 1: Failing tests.** `owner-actions.test.ts`:

```ts
import { ownerPanelMode } from "@/lib/owner-actions";

it("published + isOwner → published mode", () =>
  expect(ownerPanelMode("published", true)).toBe("published"));
it("published without owner → null", () =>
  expect(ownerPanelMode("published", false)).toBeNull());
it("draft still draft (ownership implied by visibility)", () =>
  expect(ownerPanelMode("draft", false)).toBe("draft"));
```

Extend `owner-event-actions.test.tsx` (renderToStaticMarkup pattern): published+isOwner markup contains «Редактировать» and «Мои события» and NOT «Опубликовать»; and a SignupCTA-level test is impossible without DOM — instead add a pure helper `showSignupControls(event)` in `frontend/lib/signup-availability.ts` returning `false` when `event.isOwner`, and test that.

- [ ] **Step 2: Run** — FAIL.

- [ ] **Step 3: Implement.**
  - `owner-actions.ts`: second param `isOwner = false`; keep `draft`/`pending_review` behavior status-driven (server only serves those to the owner), add `if (status === "published" && isOwner) return "published"`.
  - `OwnerEventActions.tsx`: caption for the new mode — `mode === "published" ? "Ваше событие — опубликовано" : …`; buttons: «Редактировать» + «Мои события» (no Publish button).
  - `signup-availability.ts`: `export function showSignupControls(event: { isOwner?: boolean }) { return !event.isOwner; }`
  - `SignupCTA.tsx` first lines of the render path:

```tsx
if (!showSignupControls(event)) {
  return <p className="text-[12px] uppercase tracking-wide text-ink/60">Это ваше событие</p>;
}
```

  - `EventDetailView.tsx` L108 already always mounts `OwnerEventActions`; pass `isOwner` via the event object (no prop change needed).

- [ ] **Step 4: Run** `npx vitest run` — PASS. **Step 5: Commit** `fix(events): owner sees management panel on published event, never the signup CTA`

---

### Task 7: Frontend — visible form errors on login/registration (bug №4)

**Files:**
- Modify: `frontend/components/AuthForm.tsx` (error rendering L98, submit L43–69)
- Modify: `frontend/lib/api.ts` (`loginWithPassword` L451–465, `registerWithPassword` L430–444 — already russified; add a default clause message if missing)
- Test: new `frontend/lib/__tests__/auth-error-copy.test.ts` if you extract copy; component markup test `frontend/components/__tests__/auth-form-error.test.tsx`

**Interfaces:**
- Produces: error block markup `role="alert"` with class `text-[13px] text-signal` — relied on by retest (Task 17: empty/wrong submit shows a message).

- [ ] **Step 1: Failing markup test** (`renderToStaticMarkup` of `<AuthForm mode="login" />` cannot trigger state; instead extract the error renderer):

In `AuthForm.tsx` export a tiny presentational piece:

```tsx
export function FormError({ error }: { error: string | null }) {
  if (!error) return null;
  return (
    <p role="alert" className="border border-signal px-3 py-2 text-[13px] leading-snug text-signal">
      {error}
    </p>
  );
}
```

Test:

```tsx
import { renderToStaticMarkup } from "react-dom/server";
import { FormError } from "@/components/AuthForm";

it("renders alert with message", () => {
  const html = renderToStaticMarkup(<FormError error="Неверный email или пароль" />);
  expect(html).toContain('role="alert"');
  expect(html).toContain("Неверный email или пароль");
});
it("renders nothing when null", () =>
  expect(renderToStaticMarkup(<FormError error={null} />)).toBe(""));
```

- [ ] **Step 2: Run** — FAIL. **Step 3: Implement:** replace the bare 11px `<p>` at L98 with `<FormError error={error} />` placed **directly above the submit button**; ensure inputs carry `required` (login email+password, register all four) so native validation blocks truly-empty submits; in the `catch`, keep the existing message fallback «Не удалось войти. Попробуйте ещё раз.».

- [ ] **Step 4:** In `lib/api.ts` make sure `loginWithPassword` has an `else` branch: `Не удалось войти (N)` — so a 500 never surfaces as raw English.

- [ ] **Step 5: Run** vitest — PASS. **Step 6: Commit** `fix(auth): failed login/registration always shows a visible error`

---

### Task 8: Frontend — verify-funnel returns to the origin page (bug №7)

**Files:**
- Modify: `frontend/components/AuthForm.tsx` (post-register panel link, L71–86)
- Modify: `frontend/components/VerifyEmailBanner.tsx`, `frontend/components/VerifyEmailInterstitial.tsx` (their `/auth/verify` links)
- Test: extend `frontend/lib/__tests__/safe-next.test.ts` usage — add pure helper test

**Interfaces:**
- Consumes: `safeNextPath` (already in `frontend/lib/safe-next.ts`), `usePathname()`.
- Produces: helper `verifyHref(pathname: string | null): string` in `frontend/lib/verify-link.ts` → `/auth/verify?next=<encoded pathname>` (or plain `/auth/verify` for `/`, `/login`, `/signup`, `/auth/*`). Verify page already honors `?next=` (`app/auth/verify/page.tsx` L48–50) — no change there.

- [ ] **Step 1: Failing test** `frontend/lib/__tests__/verify-link.test.ts`:

```ts
import { verifyHref } from "@/lib/verify-link";

it("carries event page into next", () =>
  expect(verifyHref("/events/abc")).toBe("/auth/verify?next=%2Fevents%2Fabc"));
it("drops auth pages and root", () => {
  expect(verifyHref("/")).toBe("/auth/verify");
  expect(verifyHref("/login")).toBe("/auth/verify");
  expect(verifyHref(null)).toBe("/auth/verify");
});
```

- [ ] **Step 2: Run** — FAIL. **Step 3: Implement** `lib/verify-link.ts`:

```ts
const SKIP = [/^\/$/, /^\/login/, /^\/signup/, /^\/auth\//];
export function verifyHref(pathname: string | null): string {
  if (!pathname || SKIP.some((re) => re.test(pathname))) return "/auth/verify";
  return `/auth/verify?next=${encodeURIComponent(pathname)}`;
}
```

Use it in AuthForm's «Ввести код» link (`usePathname()`), the banner's «Подтвердить» link, and the interstitial. AuthForm is also embedded in the event-page modal — `usePathname()` there returns the event URL, which is exactly what we want.

- [ ] **Step 4: Run** vitest — PASS. **Step 5: Commit** `fix(verify): after email confirmation user returns to the page they started from`

---

### Task 9: Frontend — stale «почта не подтверждена» banner hardening (bug №8)

**Files:**
- Modify: `frontend/lib/auth-context.tsx` (mount effect L114–128, `refresh` L184–196)
- Test: `frontend/lib/__tests__/verify-banner-visibility` (exists — extend)

**Interfaces:**
- Produces: `getMe()` failure (network/5xx) leaves `roleResolved=false` → banner predicate `ready && isAuthed && roleResolved && !emailVerified` stays false; `getMe()` 401 clears the session (`clearSession()` from `lib/auth.ts`). No consumer changes.

- [ ] **Step 1: Failing test** — extend `components/verify-banner-visibility` tests: predicate false when `roleResolved=false` (may already exist; add the case if missing). For auth-context, the testable unit is a new pure reducer `resolveMeResult(result)` if extraction is cheap; if not, rely on the predicate test + manual retest.

- [ ] **Step 2: Implement** in `auth-context.tsx`: in the mount/`refresh` `catch`: if error carries HTTP 401 (reuse `extractHttpStatus` from `lib/rsvp-errors.ts`), call `clearSession()`; otherwise keep `roleResolved` false (do NOT set it true on failure). Verify current code doesn't set `roleResolved=true` in `finally` — if it does, move it to the success path only.

- [ ] **Step 3: Run** vitest — PASS. **Step 4: Commit** `fix(auth): unverified banner never shows from stale/failed profile fetch; 401 clears session`

---

### Task 10: Frontend — follower count updates + readable «Вы подписаны» (bug №9)

**Files:**
- Modify: `frontend/components/PublicOrganizerView.tsx` (`toggleFollow` L90–103, counter L179–185, CTA L209–221)
- Test: new `frontend/lib/__tests__/follow-count.test.ts` (extract pure adjuster)

**Interfaces:**
- Produces: `adjustedFollowers(base: number | undefined, serverFollowing: boolean, override: boolean | null): number` in `frontend/lib/follow-count.ts`.

- [ ] **Step 1: Failing test:**

```ts
import { adjustedFollowers } from "@/lib/follow-count";

it("optimistic +1 when following overrides server false", () =>
  expect(adjustedFollowers(0, false, true)).toBe(1));
it("optimistic -1 floors at 0", () =>
  expect(adjustedFollowers(0, true, false)).toBe(0));
it("no override → base", () =>
  expect(adjustedFollowers(5, true, null)).toBe(5));
```

- [ ] **Step 2: Run** — FAIL. **Step 3: Implement:**

```ts
export function adjustedFollowers(base: number | undefined, serverFollowing: boolean, override: boolean | null): number {
  const b = base ?? 0;
  if (override === null || override === serverFollowing) return b;
  return Math.max(0, b + (override ? 1 : -1));
}
```

In `PublicOrganizerView`: counter tile renders `tileCount(adjustedFollowers(org.followers_count, org.is_following ?? false, followOverride?.id === id ? followOverride.value : null))`; in `toggleFollow` success path add `useQueryClient().invalidateQueries({ queryKey: ["public-organizer", id] })`. CTA subscribed-state: replace the disabled-grey look with `border border-ink bg-paper text-ink` + label «Вы подписаны ✓» (still clickable → unfollow).

- [ ] **Step 4: Run** vitest — PASS. **Step 5: Commit** `fix(organizers): follower count reflects follow/unfollow; subscribed state readable`

---

### Task 11: Frontend — mobile header cleanup + chips scroll affordance (bug №10)

**Files:**
- Modify: `frontend/components/ui/AppHeader.tsx` (mobile block L73–82, wordmark L44–56)
- Modify: `frontend/components/ui/AuthNavControl.tsx` (email span L26–31)
- Modify: `frontend/components/DiscoveryFeed.tsx` (chips row L199–238) + the shared chip-row spots (MeProfile L243–255, MyEventsBrowse L338–359) — extract one wrapper
- Test: `frontend/components/__tests__/app-header-caption.test.tsx` (extend), new `chip-row.test.tsx`

**Interfaces:**
- Produces: `frontend/components/ui/ChipRow.tsx` — `export function ChipRow({ children }: { children: React.ReactNode })` rendering the scrollable row with a right-edge fade.

- [ ] **Step 1: Failing tests.** `chip-row.test.tsx` (static markup): output contains `overflow-x-auto` and the fade element (`data-fade` attr). Header test: mobile caption block contains `truncate` class on the email slot and `gap-` between wordmark and caption.

- [ ] **Step 2: Run** — FAIL. **Step 3: Implement.**
  - `AppHeader.tsx` mobile row: wordmark gets `shrink-0 pr-3`; caption `whitespace-nowrap`; the actions container `flex min-w-0 items-center gap-2` so children can truncate.
  - `AuthNavControl.tsx`: email span → `max-w-[110px] truncate sm:max-w-none` (keeps full email on desktop); «Выйти» button `shrink-0`.
  - `CityControl` trigger: `whitespace-nowrap` (no wrap of «МСК ↓»).
  - `ChipRow.tsx`:

```tsx
export function ChipRow({ children }: { children: React.ReactNode }) {
  return (
    <div className="relative">
      <div className="flex items-stretch gap-[10px] overflow-x-auto px-[20px] py-[9px] [scrollbar-width:none] [&::-webkit-scrollbar]:hidden">
        {children}
      </div>
      <div data-fade aria-hidden
        className="pointer-events-none absolute inset-y-0 right-0 w-8 bg-gradient-to-l from-paper to-transparent sm:hidden" />
    </div>
  );
}
```

  Swap the three call-sites' raw rows for `ChipRow` (keep their inner chips markup; DiscoveryFeed keeps its border-b on an outer wrapper).

- [ ] **Step 4: Run** vitest — PASS. **Step 5: Commit** `fix(mobile): header no longer collides/truncates actions; filter chips get scroll fade`

---

### Task 12: Frontend — «Я»-hub organizer section + create CTA + calendar link (bug №11, фича)

**Files:**
- Modify: `frontend/components/MeProfile.tsx` (identity strip area L209–240 / below tabs)
- Modify: `frontend/components/MyEventsBrowse.tsx` (row: заявки chip; FILTERS labels — Task 14 handles labels)
- Test: new `frontend/components/__tests__/me-organizer-section.test.tsx`

**Interfaces:**
- Consumes: `getMyOrganizer()` from `lib/api.ts` (grep exact name — OrganizerHub uses it, `verification_status` field), `fetchMyEvents()`, `event.pendingApplicationsCount` (Task 5).
- Produces: presentational `export function OrganizerSection({ hasOrganizer, eventsCount, pendingApplications }: { hasOrganizer: boolean; eventsCount: number; pendingApplications: number })` in `frontend/components/MeOrganizerSection.tsx`.

- [ ] **Step 1: Failing markup test:**

```tsx
import { renderToStaticMarkup } from "react-dom/server";
import { OrganizerSection } from "@/components/MeOrganizerSection";

it("always offers create", () => {
  const html = renderToStaticMarkup(
    <OrganizerSection hasOrganizer={false} eventsCount={0} pendingApplications={0} />);
  expect(html).toContain("/events/new");
  expect(html).toContain("Создать событие");
});
it("shows cabinet links and applications badge when organizer", () => {
  const html = renderToStaticMarkup(
    <OrganizerSection hasOrganizer={true} eventsCount={3} pendingApplications={2} />);
  expect(html).toContain("/organizer");
  expect(html).toContain("/events/mine");
  expect(html).toContain("Заявки · 2");
});
```

- [ ] **Step 2: Run** — FAIL. **Step 3: Implement** `MeOrganizerSection.tsx`: heading «ОРГАНИЗУЮ» in the profile's label style; when `hasOrganizer` — three link-tiles (Кабинет → `/organizer`, Мои события · N → `/events/mine`, Заявки · N → `/organizer/applications`, the last with `text-signal` when N>0); always a full-width black CTA «+ Создать событие» → `/events/new`. Add a «Календарь →» link-tile next to the counters strip (`/me/calendar`) so calendar is reachable on mobile.

- [ ] **Step 4: Wire in `MeProfile.tsx`:** queries `["my-organizer"]` → `getMyOrganizer()` and `["my-events"]` → `fetchMyEvents()`, both `enabled: authed`, tolerant of 404 (no organizer yet → `hasOrganizer=false`); `pendingApplications = sum(ev.pendingApplicationsCount)`. Render the section below the tabs block.

- [ ] **Step 5: Run** vitest — PASS. **Step 6: Commit** `feat(me): profile hub gains Организую section, create-event CTA, calendar link`

---

### Task 13: Frontend — honest publish/moderation copy (bug №3 copy side)

**Files:**
- Modify: `frontend/components/PublishEventButton.tsx` (confirm dialog copy)
- Modify: the wizard footer «МОДЕРАЦИЯ ДО 24 ЧАСОВ» — grep `МОДЕРАЦИЯ ДО` in `frontend/` (wizard component under `app/events/new/` or `components/`)
- Test: new `frontend/lib/__tests__/publish-copy.test.ts`

**Interfaces:**
- Consumes: `getMyOrganizer().verification_status` (`"verified"` etc.).
- Produces: `publishDialogCopy(verified: boolean)` in `frontend/lib/publish-copy.ts` → `{ title: string; body: string }`.

- [ ] **Step 1: Failing test:**

```ts
import { publishDialogCopy } from "@/lib/publish-copy";

it("verified → instant-publish copy", () =>
  expect(publishDialogCopy(true).body).toContain("сразу появится в ленте"));
it("unverified → moderation copy", () =>
  expect(publishDialogCopy(false).body).toContain("модерации"));
```

- [ ] **Step 2: Run** — FAIL. **Step 3: Implement:**

```ts
export function publishDialogCopy(verified: boolean) {
  return verified
    ? { title: "Опубликовать событие?", body: "Событие сразу появится в ленте. Его можно будет отредактировать или вернуть в черновики." }
    : { title: "Отправить на модерацию?", body: "Событие уйдёт на модерацию — обычно это занимает до 24 часов. После одобрения оно появится в ленте." };
}
```

`PublishEventButton` fetches `["my-organizer"]` (cache-shared with Task 12) and uses the copy; wizard footer line shows «МОДЕРАЦИЯ ДО 24 ЧАСОВ» only for unverified, «ПУБЛИКАЦИЯ — СРАЗУ» for verified. After publish, if response event `moderation_required` — show the pending panel state (OwnerEventActions already handles `pending_review` via reload).

- [ ] **Step 4: Run** vitest — PASS. **Step 5: Commit** `fix(copy): publish dialog and wizard tell the truth about moderation`

---

### Task 14: Frontend — human status labels everywhere (bug №12, фича)

**Files:**
- Modify: `frontend/lib/rsvp-labels.ts` (L10–18)
- Modify: `frontend/components/MyEventsBrowse.tsx` (`FILTERS` L35–46 shortLabels; «Действия ···» button L218–226 / L239–249)
- Test: `frontend/lib/__tests__/rsvp-labels.test.ts` (extend), `frontend/components/__tests__/my-events-row-actions.test.tsx` (extend)

**Interfaces:**
- Produces: `rsvp-labels`: `going/accepted → { long: "Вы идёте", short: "Иду" }`, `applied → { long: "Заявка на рассмотрении", short: "Ждём" }` (keep the rest). `FILTERS` shortLabels → full words: `Все / Опубликованные / На проверке / Черновики / Отменённые` (row scrolls via ChipRow from Task 11).

- [ ] **Step 1: Update the failing expectations** in `rsvp-labels.test.ts` (change asserted strings first — they fail against current code).

- [ ] **Step 2: Run** — FAIL. **Step 3: Implement** label maps; `MyEventsBrowse` chips use `f.label` on all breakpoints (drop `shortLabel` split or set shortLabel=label); «Действия» control becomes a bordered chip: `border border-ink px-2 py-1 text-[12px] uppercase` with content `Действия ▾` (flip to `▴` when `expandedId` matches). Also surface the заявки counter chip on rows: `{ev.pendingApplicationsCount > 0 && <span className="pill …">Заявки · {ev.pendingApplicationsCount}</span>}` (desktop «Записи» column + mobile row line).

- [ ] **Step 4: Run** vitest — PASS. **Step 5: Commit** `feat(labels): human statuses — Вы идёте, На проверке, real filter names, actions affordance`

---

### Task 15: Frontend — /map height fix + URL redirects (фичи, bug №12 tail)

**Files:**
- Modify: `frontend/components/MapBrowse.tsx` (`mapPane` L229–255)
- Modify: `frontend/components/map/YandexMap.tsx` (wrapper L266)
- Modify: `frontend/next.config.ts` (add `redirects()`)
- Test: `frontend/components/__tests__/map-browse-control.test.ts` area — add markup assertion

**Interfaces:** n/a (CSS + config).

- [ ] **Step 1: Failing markup test:** render the mapPane wrapper (extract if needed) and assert it contains `flex-1`; assert YandexMap wrapper class contains `h-full`.

- [ ] **Step 2: Run** — FAIL. **Step 3: Implement:**
  - `MapBrowse.tsx` L230: `"relative min-h-0"` → `"relative min-h-0 flex-1"`.
  - `YandexMap.tsx` L266: `<div className="relative">` → `<div className="relative h-full w-full">`. Check `VenueMap` (event page, fixed `h-[190px]` on the inner el) still renders — inner explicit height wins; run the existing `yandex-map-placeholder` test.
  - `next.config.ts`:

```ts
async redirects() {
  return [
    { source: "/auth/signup", destination: "/signup", permanent: false },
    { source: "/events/create", destination: "/events/new", permanent: false },
  ];
},
```

- [ ] **Step 4: Run** `npx vitest run` — PASS. **Step 5: Commit** `fix(map): mobile/desktop map pane gets real height; legacy URLs redirect`

---

### Task 16: Full local verification

**Files:** none (verification gate).

- [ ] **Step 1:** `cd backend && go build ./... && go test ./...` — all PASS.
- [ ] **Step 2:** `cd frontend && npx vitest run` — all PASS (including the pre-existing 17 component + ~52 lib tests).
- [ ] **Step 3:** `cd frontend && npx next build` — compiles, no type errors.
- [ ] **Step 4:** Local smoke if the local Docker stack cooperates (memory: flaky — host-run workaround in `lia-dev-gotchas`); otherwise proceed, prod retest covers behavior.
- [ ] **Step 5: Commit** any straggler fixes; push to `origin/main`.

---

### Task 17: Deploy to prod (vdska2)

**Files:** none in repo (ops). Follow the current runbook pattern — `docs/superpowers/runbooks/2026-08-14-qa-14-aug-deploy.md` (or nearest dated deploy runbook) for exact compose invocations.

- [ ] **Step 1: Build backend** (linux/amd64) on Mac, image tag `backend-app:qa23-r1`; `docker save … | ssh vdska2 docker load`.
- [ ] **Step 2: Tag rollback from the RUNNING container** on the box (`docker inspect` the live `backend-app-1` image id → `docker tag <id> backend-app:rollback-qa23-20260823`), then `docker tag backend-app:qa23-r1 backend-app:latest`.
- [ ] **Step 3: Recreate app** with ALL four compose files + `--no-build` (exact command from the runbook). No migrations this release.
- [ ] **Step 4: Build frontend** with BOTH build args `NEXT_PUBLIC_API_URL=https://api.presence.tarski.ru` and `NEXT_PUBLIC_YANDEX_MAPS_KEY=<from box .env>`; tag `lia-frontend:qa23-r1`; save|load; tag rollback from running `lia-frontend-presence`; recreate container on :3002.
- [ ] **Step 5: Smoke:** `curl -sf https://presence.tarski.ru >/dev/null && curl -sf https://api.presence.tarski.ru/api/v1/events >/dev/null`; `docker logs --since 5m` both containers — no error spam.
- [ ] **Step 6:** Prune per `lia-deploy-image-cleanup` memory (builder prune, dangling images, keep last ~3 rollback tags).

---

### Task 18: Prod re-test of every fixed item

**Files:** append results to `docs/superpowers/runbooks/2026-08-23-full-role-qa-run.md` (section «Re-test after fixes»).

Use live test accounts `dodonopavel+qa23v@gmail.com` (зритель) / `dodonopavel+qa23o@gmail.com` (организатор, **unverified**) and admin `poulissimo@gmail.com`; verification codes from `gateguard.users.email_verification_token` if needed. Mobile viewport 390×900 first.

- [ ] №4: /login — submit empty → native validation blocks; wrong password → visible «Неверный email или пароль».
- [ ] №3: as qa23o create event «QA-retest-23aug» → publish → dialog says «на модерацию» → status `pending_review` in DB, NOT in public feed; owner sees «На модерации» panel. Admin: event in moderation tab («НА ПРОВЕРКЕ») → Approve → appears in feed.
- [ ] №5/№6: as qa23o open own published event → owner panel visible (Редактировать/Мои события), NO «Записаться»; API check: `POST /events/{id}/rsvp` with owner token → 403.
- [ ] №7: fresh signup `dodonopavel+qa23r@gmail.com` from an event page modal → after code entry lands back on that event.
- [ ] №8: login admin → no «почта не подтверждена» banner.
- [ ] №9: as qa23v follow an organizer → counter +1 immediately; «Вы подписаны ✓» readable; unfollow → back.
- [ ] №10: 390px — header: PRESENCE / caption not colliding, «Выйти» visible, email truncated; chips row shows right-edge fade.
- [ ] №11: /me as qa23o → «Организую» section (Кабинет, Мои события, Заявки, + Создать событие) + «Календарь» link.
- [ ] №12: /map — map tiles RENDER on mobile and desktop; «Всего» matches feed count logic (no past events); /events/mine chips read Опубликованные/На проверке/Черновики; profile record shows «Иду»; `/auth/signup` and `/events/create` redirect.
- [ ] Заявки counter: qa23v applies to the retest event (after approve) → qa23o `/events/mine` row shows «Заявки · 1»; /me shows it too.
- [ ] **Cleanup:** reject/cancel the retest event, delete retest rsvps/follows via SQL (pattern from the 23-aug runbook), delete `+qa23r` verification leftovers if any; leave accounts; browser back to admin.
- [ ] Update the runbook + memory (`lia-qa-23aug-full-role-run`) with deploy tags and retest outcomes.

---

## Self-Review notes

- Spec coverage: №3→T1+T13, №4→T7, №5→T2+T6, №6→T4+T5+T6, №7→T8, №8→T9, №9→T10, №10→T11, №11→T12, №12→T3+T14+T15 (+«Иду» label), фичи: hub→T12, dup-event→already exists as «Копия» in MyEventsBrowse desktop L210 — T14's mobile «Действия» panel must include it (verify «Копия» is inside `OverflowExtras` for mobile; if not, add the same link there — covered in T14 step 3).
- «Счётчик новых заявок» → T4 (backend) + T5 + T12/T14 (display).
- Type consistency: `event.isOwner` / `pendingApplicationsCount` introduced in T5, consumed in T6/T12/T14; `ownerPanelMode(status, isOwner)` signature change updates its existing test file in T6; `verifyHref`, `publishDialogCopy`, `adjustedFollowers`, `showSignupControls`, `ChipRow`, `OrganizerSection` all defined before use.
- Line numbers are as of commit `0db0ce3` — treat as anchors, re-locate by identifier if drifted.
