import { CreateEventForm } from "@/components/CreateEventForm";

/** O2 create wizard — Swiss Grid chrome lives inside CreateEventForm (AppHeader + ORG_NAV). */
export default function CreateEventPage() {
  return <CreateEventForm />;
}
